<!DOCTYPE html>
<html>
  <head>
    <title>CampaignActionItems</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= ["#{controller_path.tr('/', '-')}-page", ('admin-training-page' if controller_path.start_with?('admin_training/'))].compact.join(' ') %>">
    <%= yield %>
  </body>
</html>
